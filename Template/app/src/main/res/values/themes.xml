<resources>

    <!-- Overwrite the ToggleButton style -->
    <style name="Widget.Button.Toggle" parent="android:Widget">
        <item name="android:background">@drawable/btn_toggle_bg</item>
        <item name="android:disabledAlpha">?android:attr/disabledAlpha</item>
    </style>

    <style name="YourThemeName" parent="@android:style/Theme.Black">
        <!-- Tell Android to use a custom style when creating a ToggleButton -->
        <item name="android:buttonStyleToggle">@style/Widget.Button.Toggle</item>
        <!-- Perhaps more theme items here...? -->
    </style>

    <style name="MyColor" parent="android:Widget.Button">
        <item name="android:textColor">@color/red</item>
        <item name="android:colorButtonNormal">@color/green</item>
        <item name="android:textAlignment">textStart</item>
    </style>

</resources>
